# Dev-only dependencies — NOT deployed to Lambda or EMR
# Install with: pip install -r requirements-dev.txt
#
# Includes everything in requirements.txt plus:
#   - pyspark: local Spark testing without a cluster
#   - moto:    AWS mock library for deeper integration mocking
#   - black:   opinionated formatter (optional — ruff format is sufficient)

-r requirements.txt

# Local Spark testing (matches EMR Serverless emr-7.0.0 Spark version)
pyspark==3.5.0

# AWS mocking (useful for deeper Lambda integration tests without real AWS)
moto[kinesis,dynamodb,s3]>=5.0.0

# Type checking
mypy>=1.8.0
boto3-stubs[kinesis,dynamodb,s3,emr-serverless,athena]>=1.34.0
