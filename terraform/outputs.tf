# All outputs are defined in terraform/main.tf alongside the modules.
# This file exists as a reminder that outputs.tf is optional when modules
# are referenced inline — see main.tf output blocks.
#
# To view all outputs after apply:
#   terraform output
#   terraform output -raw emr_application_id
