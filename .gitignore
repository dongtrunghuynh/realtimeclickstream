# Python
__pycache__/
*.pyc
*.pyo
*.pyd
.Python
*.egg-info/
dist/
build/
.venv/
venv/
env/
uv.lock

# Environment / secrets — NEVER commit these
.env
.env.*
*.env

# AWS credentials
.aws/

# Dataset — too large for git
data/sample/*.csv
data/sample/*.parquet

# Terraform
terraform/.terraform/
terraform/.terraform.lock.hcl
terraform/terraform.tfstate
terraform/terraform.tfstate.backup
terraform/*.tfplan


# Lambda build artifacts
src/lambda/*.zip

# AWS CLI installer — downloaded during setup, not part of the repo
awscliv2.zip
aws/
src/lambda/sessionizer/package/

# Test artifacts
.pytest_cache/
.coverage
htmlcov/
coverage.xml

# IDE
.idea/
.vscode/
*.swp
*.swo

# macOS
.DS_Store

# Dashboard outputs
src/dashboard/*.csv
