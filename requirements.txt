# Core AWS SDK
boto3>=1.34.0
botocore>=1.34.0

# Data processing
pandas>=2.1.0
pyarrow>=14.0.0        # Parquet read/write for Lambda Layer + local dev

# Linting & testing (dev only — not deployed to Lambda)
ruff>=0.3.0
pytest>=8.0.0
pytest-cov>=4.1.0

# Dashboard
tabulate>=0.9.0        # Pretty-print tables in the accuracy dashboard
